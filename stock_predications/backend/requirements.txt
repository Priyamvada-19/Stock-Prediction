fastapi>=0.100.0
uvicorn>=0.20.0
pandas>=2.0.0

scikit-learn>=1.0.0
numpy>=1.20.0
joblib>=1.0.0
yfinance>=0.2.0
matplotlib>=3.0.0
seaborn>=0.11.0

#backend commands
pip install -r backend\requirements.txt
cd backend
python -m uvicorn main:app --reload

#frontend commands
#simply run the index.html file in the frontend folder using a web browser, or simply click GO LIVE on the bottom right corner of the VS code if you have the Live Server extension installed. This will open the frontend in your default web browser.